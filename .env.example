POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=clone_social
POSTGRES_USER=devuser
POSTGRES_PASSWORD=devpass

# MongoDB (caso adicione depois)
MONGO_URL=mongodb://root:rootpass@localhost:27017

# Ambiente
ENVIRONMENT=dev

# Banco de dados usado pelo N8N (se quiser usar a mesma conexão no backend pra buscar execuções, logs etc)
N8N_DB_URL=postgresql://devuser:devpass@localhost:5432/n8n_data

# Autenticação básica do N8N (caso precise em chamadas externas)
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=senha123

# Tunnel externo (ex: ngrok)
WEBHOOK_TUNNEL_URL=https://seu-ngrok.ngrok.io

# OpenAI (substitua se for usar Assistant ID e API Key direto)
OPENAI_API_KEY=sk-proj-XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
OPENAI_ASSISTANT_ID=asst_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

LOG_LEVEL=DEBUG

